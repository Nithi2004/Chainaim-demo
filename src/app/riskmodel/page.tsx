"use client"; // Ensure this is a client component

import RiskModel from "../components/RiskModel"; // Adjust the import path based on your folder structure

const RiskModelPage = () => {
  return <RiskModel />;
};

export default RiskModelPage;
